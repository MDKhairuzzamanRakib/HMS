@model Master_Details.Models.Department

<div class="row mb-3">
    <div class="col-10">
        @Html.DropDownListFor(x => x.DepartmentId, ViewBag.departments as SelectList, "-Selct-", htmlAttributes: new { @class = "form-control mb-2" })
    </div>
</div>
